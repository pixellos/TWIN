@model string

<h3>
    Welcome back, @Model
    @Html.ActionLink("Sign Out", "Logout", "Account", null, new { @class = "btn btn-primary", @id = "logout-btn", @style= "position: absolute; right: 10px;" })
</h3>
